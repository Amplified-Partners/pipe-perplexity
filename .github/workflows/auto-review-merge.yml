# Retired 2026-07-26: the staged DeepSeek/Kimi/OpenAI validator pipeline in
# openai-review.yml replaces this legacy single-provider reviewer and its
# dead AUTO_MERGE_PAT posting path. Kept as a tombstone for audit history.
name: legacy-ai-review-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired; use validator-pipeline / review."
